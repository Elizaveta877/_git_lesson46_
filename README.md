Асинхронна логіка: Додано createAsyncThunk для виконання HTTP-запитів до JSONPlaceholder API.

Керування станами запиту: У слайсі реалізовано обробку трьох життєвих циклів запиту:

pending: Відображення індикатора завантаження.

fulfilled: Успішне отримання даних та оновлення стану.

rejected: Обробка помилок у разі збою мережі.

Взаємодія компонентів:

ComponentC тепер ініціює завантаження даних при монтуванні (useEffect).

Додано можливість динамічного оновлення даних (завантаження випадкового користувача).

Встановлення та запуск
Клонувати репозиторій:

Bash

git clone https://github.com/ваш-логін/назва-репозиторію.git
Перейти в директорію проєкту:

Bash

cd назва-папки
Встановити залежності:

Bash

npm install
Запустити проєкт:

Bash

npm run dev
## 🚀 Демо-версія

-
